20. Valid Parentheses


Solution:

Time Complexity - O(n)
Space Complexity - O(n)

```
class Solution:
    def isValid(self, s: str) -> bool:
        stack = []
        for i in s:
            if stack == []:
                stack.append(i)
            else:
                if i == ')' and stack[-1] == '(':
                    stack.pop()
                elif i == '}' and stack[-1] == '{':
                    stack.pop()
                elif i == ']' and stack[-1] == '[':
                    stack.pop()
                else:
                    stack.append(i)
        if len(stack) == 0:
            return True
        else:
            return False
```
