33. Search in Rotated Sorted Array

Solution - 
Time Complexity - O(n)
Space Complexity - O(1)

```
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        #using binary search to figure out the minimum element in the current element

        l, h = 0, len(nums)-1
        while l < h:
            mid = l + (h - l) // 2
            
            if nums[mid] == target:
                return mid
            
            if nums[l] <= nums[mid]:
                if target >= nums[l] and target < nums[mid]:
                    h = mid - 1
                else:
                    l = mid + 1
            else:
                if target <= nums[h] and target > nums[mid]:
                    l = mid + 1
                else:
                    h = mid - 1
        
        return l if target == nums[l] else -1

```
