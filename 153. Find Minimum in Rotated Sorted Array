153. Find Minimum in Rotated Sorted Array

Solution - 
Time complexity - O(log n)
Space Complexity - O(1)

```
class Solution:
    def findMin(self, nums: List[int]) -> int:
        #using binary search to figure out the minimum element in the current element
        l, h = 0, len(nums)-1

        while l < h:
            mid = l + (h - l) // 2 #handling interger overflow

            if nums[mid] < nums[h]: # if mid is less than high (h) then we got our right window limit else we have left window limit
                h = mid
            else:
                l = mid + 1
        
        return nums[h]


```
